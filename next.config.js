/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "images.unsplash.com",
      "media.timeout.com",
      "cdn.britannica.com",
      "img.freepik.com",
    ],
  },
};

module.exports = nextConfig;
